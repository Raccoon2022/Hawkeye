
// Will just be ESP32
// to do:
/*


*/

// Variables
int rocket_state = 0; // 0 is on pad, 1 is in flight

void setup() {
  // put your setup code here, to run once:

}

void loop() {
  // put your main code here, to run repeatedly:
  
  /*
  switch
    case 0:
      if (accel magnitude != gravity){
        rocket_state = 1
      }
    case 1:
      if (accel magnitude == gravity && (t1 <= time_since_launch <= t2)){
        trigger camera and parachute pyrocharge
      }
  
  */
  tempMethod();

}

void tempMethod(){
  printf("Hello World!");
}
